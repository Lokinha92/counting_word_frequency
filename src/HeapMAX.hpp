#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

struct Info {
    string palavra;
    int freq;

    Info(const string& p, int f) : palavra(p), freq(f) {}
};

class HeapMAX
{
private:
    
public:
    
};
